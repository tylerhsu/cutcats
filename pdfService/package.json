{
  "name": "pdf-service",
  "version": "1.0.0",
  "description": "PDF generation service using AWS lambda",
  "main": "index.js",
  "scripts": {
    "deploy": "serverless deploy",
    "invoke": "serverless invoke --function generatePdf",
    "logs": "serverless logs --function generatePdf --tail",
    "log": "npm run logs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tyler Hsu",
  "license": "ISC",
  "dependencies": {
    "pdfmake": "^0.1.37",
    "serverless": "^1.30.1"
  }
}
